export interface Referrals {
  readonly id: string;
  readonly accountId: string;
  readonly referredNumber: string;
  readonly status: boolean;
  readonly referido: any;
  readonly createdAt: string;
  readonly updatedAt: string;
}
